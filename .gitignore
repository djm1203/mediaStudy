# Rust build artifacts
/target/
**/*.rs.bk
# Cargo.lock is committed: this is a binary crate and reproducible release builds need it.

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Local config with secrets
config.toml

# Data directory (user content)
/data/

# Embedding model cache (downloaded at runtime, ~90MB)
.fastembed_cache/

# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm
# >>> BEACON Framework >>>
.beacon-session.json
.claude/settings.local.json
# <<< BEACON Framework <<<

# BEACON framework + internal governance/planning docs — kept local, not published.
# (README.md and LICENSE remain public.)
/.beacon/
/.beacon-framework.toml
/CLAUDE.md
/AGENTS.md
/docs/
